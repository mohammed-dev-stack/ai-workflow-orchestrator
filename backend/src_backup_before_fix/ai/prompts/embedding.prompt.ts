// backend/src/ai/prompts/embedding.prompt.ts

// ============================================================
// معرف وإصدار المطالبة — §6
// ============================================================

/**
 * معرف مطالبة التضمين (Embedding).
 * يُستخدم للإشارة إلى هذه المطالبة في الكود والسجلات.
 */
export const EMBEDDING_PROMPT_ID = 'embed-v1';

/**
 * إصدار مطالبة التضمين (Semantic Versioning).
 * يُستخدم لتتبع التغييرات والرجوع إلى إصدارات سابقة.
 */
export const EMBEDDING_PROMPT_VERSION = '1.0.0';

// ============================================================
// محتوى المطالبة — §6
// ============================================================

/**
 * مطالبة توليد التضمينات (Embedding).
 * تُستخدم لاستخراج تضمينات (متجهات) من النصوص باستخدام Claude.
 * 
 * المتغيرات:
 * - {{text}}: النص المراد توليد تضمين له
 * - {{dimensions}}: بُعد المتجه المطلوب (من config.vector.dimensions)
 * 
 * [مُتحقَّق منطقياً بتتبع كامل] — مطالبة مُصدرة ومُرقمة.
 */
export const embeddingPrompt = `
أنت مساعد متخصص في توليد التضمينات (embeddings) من النصوص.
مهمتك هي تحويل النص المُقدَّم إلى مصفوفة من الأرقام العشرية (float vector) 
بُعدها {{dimensions}}، والتي تمثل المعنى الدلالي للنص في فضاء متجهي.

**تعليمات:**
1. اقرأ النص المُقدَّم بعناية.
2. قم بتوليد تضمين (embedding) يمثل المعنى الدلالي للنص.
3. يجب أن يكون التضمين مصفوفة من الأرقام العشرية (float) بطول {{dimensions}}.
4. قم بإرجاع التضمين كمصفوفة JSON فقط، بدون أي تعليقات أو تفسيرات إضافية.
5. يجب أن يكون التضمين موحداً (normalized) بحيث يكون طول المتجه (norm) مساوياً لـ 1.

**النص:**
{{text}}

**تنسيق الإخراج (JSON):**
{
  "embedding": [0.123, -0.456, 0.789, ...]
}
`;

/**
 * تصدير المطالبة كافتراضي مع بياناتها الوصفية.
 */
export default {
  id: EMBEDDING_PROMPT_ID,
  version: EMBEDDING_PROMPT_VERSION,
  content: embeddingPrompt,
  description: 'مطالبة توليد التضمينات (Embedding) من النصوص باستخدام Claude',
  model: 'claude-3-sonnet-20241022',
  maxTokens: 4096,
};
