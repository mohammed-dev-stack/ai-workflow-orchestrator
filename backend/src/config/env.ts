// ============================================================
// backend/src/config/env.ts
// ============================================================
// المصدر الوحيد (SSoT) لمتغيرات البيئة.
// يُعيد تصدير الإعدادات المدققة من env.schema.ts (PostgreSQL + Prisma).
// ============================================================

import { loadEnvConfig, envSchema, EnvConfig } from './env.schema.js';

/**
 * كائن الإعدادات المُحمَّل والمُتحقَّق منه (وفقاً لـ DATABASE_URL وليس MONGO_URI).
 * يُستخدم في بقية التطبيق بدلاً من قراءة process.env مباشرة.
 */
export const env = loadEnvConfig();

// إعادة تصدير الأنواع والمخططات للاستخدام في باقي الملفات
export { envSchema, EnvConfig };

/**
 * تصدير الإعدادات كافتراضي لسهولة الاستيراد.
 */
export default env;